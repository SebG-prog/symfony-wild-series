{% extends 'base.html.twig' %}

{% block body %}
{{dump(programs)}}
<h1>{{ category.name }}</h1>

    {% for program in programs %}
    <div class="container">
        <h2>{{ program.id }}. {{ program.title }}</h2>
        <p>{{ program.summary }}</p>
    </div>
    {% endfor %}

{% endblock %}